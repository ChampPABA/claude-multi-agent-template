# Design Context: {PROJECT_NAME}

> **Auto-generated by `/csetup` or `/designsetup`**
> **Last updated:** {UPDATED_AT}

---

## 🎨 Design System Overview

**Status:** {DESIGN_SYSTEM_STATUS}
- ✅ Complete (data.yaml + README.md exist)
- ⚠️ Partial (only one file exists)
- ❌ None (needs /designsetup)

**Generated from:**
- {GENERATION_METHOD}
  - Path 1: Reference design analysis
  - Path 2: Brownfield codebase extraction
  - Path 3: AI-generated (greenfield)

---

## 📁 Design Files (Token-Efficient Loading)

### Primary Reference (Lightweight)
```
✅ data.yaml (~800 tokens)
   design-system/data.yaml

   Contains: Colors, spacing, typography, shadows, borders, animations
   Use: Quick reference for all commands and agents
```

### Human-Readable Summary (For Humans Only)
```
📖 README.md (~100 tokens)
   design-system/README.md

   Contains: Summary of design system for human review
   Use: Humans reviewing design, NOT for agents
```

### Fallback (Universal Principles)
```
📚 design/*.md (~3000 tokens total)
   .claude/contexts/design/

   Contains: Box thinking, color theory, spacing, typography (universal)
   Use: When no project-specific style guide exists
```

---

## 🎯 Quick Design Tokens

### Colors
```json
{
  "primary": "{PRIMARY_COLOR}",
  "secondary": "{SECONDARY_COLOR}",
  "background": "{BACKGROUND_COLOR}",
  "foreground": "{FOREGROUND_COLOR}"
}
```

**Tailwind Usage:**
- Primary: `bg-primary`, `text-primary`, `border-primary`
- Foreground: `text-foreground`, `text-foreground/70` (70% opacity)
- Background: `bg-background`, `bg-muted`

### Spacing Scale
```
{SPACING_SCALE}
```

**Common Patterns:**
- Component padding: `p-4` (16px) or `p-6` (24px)
- Section gap: `gap-8` (32px) or `gap-12` (48px)
- Layout margin: `mt-16` (64px) or `mt-24` (96px)

### Typography
```
Font Family: {FONT_FAMILY}
Headings: {HEADING_CLASSES}
Body: text-base (16px)
```

### Shadows
```
Cards: shadow-md
Dropdowns: shadow-lg
Modals: shadow-xl
Buttons (hover): shadow-sm
```

---

## 📦 Component Library

**Library:** {COMPONENT_LIBRARY}
**Install:** `{INSTALL_COMMAND}`

**Common Components:**
{COMPONENT_LIST}

**Where to find:**
- Shared: `components/ui/` (shadcn/ui components)
- Custom: `components/` (project-specific)

---

## ⚠️ Design Rules

### Colors
- ❌ **NO** hardcoded colors: `text-gray-500`, `#64748b`
- ✅ **USE** theme tokens: `text-foreground/70`, `bg-muted`

### Spacing
- ❌ **NO** arbitrary values: `p-5`, `gap-7`, `mt-13`
- ✅ **USE** spacing scale: `p-4`, `p-6`, `gap-8`, `mt-16`

### Consistency
- ❌ **NO** mixing patterns: Some cards use `shadow-sm`, others use `shadow-lg`
- ✅ **USE** consistent patterns: All cards use `shadow-md`

### Component Reuse
- ❌ **NO** creating duplicates: Check existing components first!
- ✅ **USE** component search: Glob + Grep before creating new

---

## 🤖 Agent Loading Instructions

### For All Commands (/pageplan, /csetup, /cdev)

**Step 1: Load this file (design-context.md)**
```
Read: .claude/contexts/domain/{PROJECT_NAME}/design-context.md
Tokens: ~1000
```

**Step 2: Load data.yaml (if needed)**
```
Read: design-system/data.yaml
Tokens: ~800
```

**Total: ~1500 tokens (efficient loading)**

---

### For uxui-frontend Agent (STEP 0.5)

**Mandatory Pre-Work:**

1. **Read design-context.md** (this file)
   - Quick tokens reference
   - File paths
   - Critical rules

2. **Read data.yaml**
   - Full design tokens
   - Component library info
   - Usage patterns

3. **Report:**
   ```
   ✅ Design Context Loaded
   📁 design-context.md ✓
   📦 data.yaml ✓

   Design Tokens Extracted:
   - Primary: {PRIMARY_COLOR}
   - Spacing: 4, 8, 16, 24, 32, 48px
   - Component Library: {COMPONENT_LIBRARY}
   ```

---

## 📊 Token Usage Comparison

| Approach | Tokens | Speed | Recommended |
|----------|--------|-------|-------------|
| **design-context.md + data.yaml** | ~1800 | Fast | ✅ |
| **data.yaml only** | ~800 | Very Fast | ✅ (quick tasks) |

---

## 🔄 When to Update This File

Run `/csetup` or `/designsetup` again when:
- ✅ Design tokens change (new colors, spacing)
- ✅ Component library changes (shadcn → Chakra UI)
- ✅ data.yaml updated
- ✅ New project phase (rebrand, redesign)

---

## 📖 References

- **Design Tokens:** `design-system/data.yaml` (agent reads this)
- **Human Summary:** `design-system/README.md` (for humans)
- **Universal Design:** `.claude/contexts/design/`
- **Best Practices:** `.claude/contexts/domain/{PROJECT_NAME}/best-practices/`

---

**🎯 Next Steps for Agents:**
1. Load design-context.md (~1K tokens) ✅
2. Load data.yaml (~800 tokens) ✅
3. Extract design tokens ✅
4. Report loaded context ✅
5. Begin implementation with design system awareness ✅
