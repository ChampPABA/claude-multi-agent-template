services:
  api:
    build: .
    ports:
      - "${API_PORT}:3000"
    env_file: .env
  db:
    image: postgres:17-alpine
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data
volumes:
  pgdata:
